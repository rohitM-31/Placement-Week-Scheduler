# Copy to .env and edit as needed.

# If set, connects to this real MongoDB instance (local or Atlas).
# If left unset, the server starts an in-memory MongoDB automatically so
# you can run the whole project with zero DB setup (data won't persist
# across restarts in that mode).
# MONGODB_URI=mongodb://127.0.0.1:27017/placement_scheduler
DATABASE_URL=mongodb+srv://rohitmoka2004_db_user:t40tYiLrDL5KyQIM@cluster0.bpdq44e.mongodb.net/?appName=Cluster0
PORT=4000
